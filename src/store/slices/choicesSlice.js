import { createSlice } from "@reduxjs/toolkit";

const initialState = {
};

const choicesSlice = createSlice({
  name: 'choices',
  initialState,
  reducers: {

  },
});

export const { } = choicesSlice.actions;

export default choicesSlice.reducer;
